# Deny execution of PHP scripts in this directory
<FilesMatch "\.php$">
    Deny from all
</FilesMatch>

# Only allow image types
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Allow from all
</FilesMatch>
